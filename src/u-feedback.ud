(section
  (ref "install_feed")
  (title "Installer feedback")

  (para "Callbacks may be registered to receive information from the
    installer library itself. This allows for the creation of progress
    bars, terminal I/O etc.")

  (para-verbatim example
    (render "u-feedback.c"))

  (para "The above program prints info and warnings. It also makes use
    of userdata.")
)
